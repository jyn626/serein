<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Win98ButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#D4D0C8"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="OuterLight"
                        BorderBrush="#FFFFFF"
                        BorderThickness="1,1,0,0"
                    >
                        <Border x:Name="InnerLight"
                            BorderBrush="#F1EFEA"
                            BorderThickness="1,1,0,0"
                        >
                            <!--#F1EFEA-->
                            <Border x:Name="OuterDark"
                                BorderBrush="#404040"
                                BorderThickness="0,0,1,1"
                            >
                                <Border x:Name="InnerDark"
                                    BorderBrush="#808080"
                                    BorderThickness="0,0,1,1"
                                    Background="{TemplateBinding Background}"
                                    Padding="2"
                                    >
                                    <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Border>
                        </Border>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="InnerDark"
                                Property="Background"
                                Value="#F1EFEA"/>
                        </Trigger>


                        <Trigger Property="IsPressed" Value="True">

                            <Setter TargetName="OuterLight"
                                    Property="BorderBrush"
                                    Value="#202020"/>

                            <Setter TargetName="InnerLight"
                                 Property="BorderBrush"
                                 Value="#404040"/>

                            
                            <Setter TargetName="OuterDark"
                                    Property="BorderBrush"
                                    Value="#FFFFFF"/>


                            <Setter TargetName="InnerDark"
                                    Property="BorderBrush"
                                    Value="#F1EFEA"/>
                            
                            <Setter TargetName="contentPresenter"
                                    Property="Margin"
                                    Value="1,1,0,0"/>

                        </Trigger>
                        
                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>